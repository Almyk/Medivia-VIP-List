<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".UI.MainActivity">

    <Button
        android:id="@+id/bt_update"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Update"
        app:layout_constraintTop_toTopOf="parent"/>

    <EditText
        android:id="@+id/et_player_name"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:hint="Player name"
        app:layout_constraintTop_toBottomOf="@id/bt_update"
        app:layout_constraintRight_toLeftOf="@id/bt_add_player"/>
    <Button
        android:id="@+id/bt_add_player"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Add Player"
        app:layout_constraintTop_toBottomOf="@id/bt_update"
        app:layout_constraintLeft_toRightOf="@id/et_player_name"/>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rv_vip"
        app:layout_constraintTop_toBottomOf="@id/bt_add_player"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp"/>

</android.support.constraint.ConstraintLayout>